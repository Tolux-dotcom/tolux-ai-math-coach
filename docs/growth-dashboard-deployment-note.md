# Deployment boundary

This branch contains implementation work only. Do not merge to `main`, apply the growth-events migration to production Supabase, or deploy the dashboard to production until the implementation PR has been reviewed and explicitly approved.
